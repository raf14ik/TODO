
  import firebase from "firebase"; 
  // import config firebase hosting..
  
  const firebaseApp = firebase.initializeApp({
    //api-firebase
  
  }); // config firebase project
  
  const db = firebaseApp.firestore();
  export default db ;
